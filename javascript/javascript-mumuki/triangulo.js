const base = Number(prompt("Ingresá el valor de la base del tríangulo"));
const height = Number(prompt("Ingrese el valor de la altura del tríangulo"));

const result = (base * height)/ 2;

alert(`El resultado del área del triángulo con base ${base} y altura ${height} es: ${result}`)
const numero = Number(prompt("Ingresá un número"));
const cantidad = Number(prompt("Ingresá una cantidad"));

const incremento1 = numero + (cantidad*1);
const incremento2 = numero + (cantidad*2);
const incremento3 = numero + (cantidad*3);
const incremento4 = numero + (cantidad*4);
const incremento5 = numero + (cantidad*5);

alert(`
-${incremento1}
-${incremento2}
-${incremento3}
-${incremento4}
-${incremento5}`);

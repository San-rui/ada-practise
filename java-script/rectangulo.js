const base = Number(prompt("Ingresá el valor de la base del rectángulo"));
const height = Number(prompt("Ingresá el valor de la altura del rectángulo"));

const result = 2*base + 2*height;

alert(`El resultado del perímetro del rectángulo con base ${base} y altura ${height} es: ${result}`)